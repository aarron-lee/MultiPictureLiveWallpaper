name: Build APK

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        variant: [free, dnt]
        build_type: [debug, release]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: gradle

      - name: Write local.properties
        run: echo "sdk.dir=$ANDROID_SDK_ROOT" > local.properties

      - name: Install Android SDK platform 34
        run: |
          yes | "${ANDROID_SDK_ROOT}/cmdline-tools/latest/bin/sdkmanager" \
            --licenses > /dev/null 2>&1 || true
          "${ANDROID_SDK_ROOT}/cmdline-tools/latest/bin/sdkmanager" \
            "platforms;android-34" \
            "build-tools;34.0.0"

      - name: Decode release keystore
        if: matrix.build_type == 'release' && secrets.RELEASE_KEYSTORE != ''
        env:
          RELEASE_KEYSTORE: ${{ secrets.RELEASE_KEYSTORE }}
        run: echo "$RELEASE_KEYSTORE" | base64 -d > release.keystore

      - name: Make gradlew executable
        run: chmod +x gradlew

      - name: Build ${{ matrix.variant }} ${{ matrix.build_type }} APK
        env:
          RELEASE_STORE_PASSWORD: ${{ secrets.RELEASE_STORE_PASSWORD }}
          RELEASE_KEY_ALIAS:      ${{ secrets.RELEASE_KEY_ALIAS }}
          RELEASE_KEY_PASSWORD:   ${{ secrets.RELEASE_KEY_PASSWORD }}
        run: |
          BUILD_TYPE="${{ matrix.build_type }}"
          TASK=":variant:${{ matrix.variant }}:assemble${BUILD_TYPE^}"

          if [[ "$BUILD_TYPE" == "release" && -f release.keystore ]]; then
            ./gradlew "$TASK" --no-daemon \
              -PRELEASE_STORE_FILE="${{ github.workspace }}/release.keystore" \
              -PRELEASE_STORE_PASSWORD="$RELEASE_STORE_PASSWORD" \
              -PRELEASE_KEY_ALIAS="$RELEASE_KEY_ALIAS" \
              -PRELEASE_KEY_PASSWORD="$RELEASE_KEY_PASSWORD"
          else
            ./gradlew "$TASK" --no-daemon
          fi

      - name: Upload ${{ matrix.variant }} ${{ matrix.build_type }} APK
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.variant }}-${{ matrix.build_type }}
          path: variant/${{ matrix.variant }}/build/outputs/apk/${{ matrix.build_type }}/*.apk
          if-no-files-found: error
          retention-days: 30
